"""TokensGate application package.

Phase 1: FastAPI skeleton, token counting, Redis integration.
Phase 2: prompt cache, sliding-window rate limiting, cost-aware routing.
Phase 3: circuit breaker (rolling window) + Ollama failover provider.
"""

__version__ = "0.3.0"
